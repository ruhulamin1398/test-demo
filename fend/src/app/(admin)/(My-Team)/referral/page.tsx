import React from "react";
import { ReferralInfoTable } from "./ReferralInfoTable";

const Referral = () => {
  return (
    <section>
      <h1 className="mb-1 mt-0 text-xl font-black">Referral User</h1>
      <ReferralInfoTable />
    </section>
  );
};

export default Referral;
