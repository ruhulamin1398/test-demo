import localFont from "next/font/local";
import { Inter } from "next/font/google";

export const SrbijaFont = localFont({ src: "./Srbija Sans.ttf" });
export const InterFont = Inter({ subsets: ["latin"] });
